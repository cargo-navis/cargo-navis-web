import { fontSize } from './fontSizes';
import { colors } from './colors';
import { borderRadius, Radius } from './radii';

export const theme = { colors, fontSize, borderRadius };

export type { Radius };