import { fontSize } from './fontSizes';
import { colors } from './colors';
import { borderRadius, Radius } from './radii';
import { spacing } from './space';

export const theme = { colors, fontSize, borderRadius, spacing };

export type { Radius };